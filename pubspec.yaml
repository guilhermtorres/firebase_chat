name: firebase_chat
description: Aplicativo de chat utilizando firebase como backend.


publish_to: 'none' 


version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  cloud_firestore: ^0.13.0+1
  image_picker: ^0.6.2+3
  google_sign_in: ^4.1.1
  firebase_storage: ^3.1.1
  firebase_auth: ^0.15.3

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true

  